# Agent 面试宝典 · 修订说明

> **v2（2026-09-09）**：v1 的必改/要改项已全部执行并入库。第一节背景概念**保留**（后面所有修改都从这里推出来）；v1 清单压缩存档见第二节；本轮（v2）意见见第三节。

---

## 一、先补两个背景概念

### 1.1 分布税：模型不是人，它只是"见过"

一句话：**模型能不能可靠地做一件事，主要取决于它在后训练阶段见没见过这类轨迹，而不是这件事本身难不难。**

这跟人的直觉是反的。人判断难度靠"这事复杂吗"，模型的成败靠"这事在训练分布里吗"。两个量几乎不相关。

#### 一个具体例子：编辑文件

coding agent 要改代码，怎么表达"改哪里"？业界主要有两种格式：

**`str_replace`（Claude 系）**——给出要被替换的原文和替换后的新文：

```json
{
  "path": "utils.py",
  "old_string": "def approximate_pi(num_points=1000000):",
  "new_string": "def approx_pi(num_points=1000000):"
}
```

要求 `old_string` 在文件里**精确唯一匹配**，空格、换行、缩进一个字符都不能差。

**`apply_patch` / V4A（GPT 系）**——一种带上下文锚点的 diff 格式，靠周围几行代码定位，而不是靠行号。

关键事实：**这两种格式各自被不同厂商重度后训练过**。Claude 系模型"想调"的就是 str_replace，GPT 系在 apply_patch 上表现最好。有生产统计显示，同一个模型换编辑格式，成功率能差几十个百分点；也有基准报告非原生格式下的 patch 失败率接近一半。

这里最反直觉的一点：**一个设计上更聪明的格式，会输给一个更笨但在分布里的格式。**

顺带一个和宝典相关的数据：str_replace 类工具在生产里的错误率约 13%，而 `read_file`、`search` 这类工具都在 1% 以下；其中 77% 的失败是"`old_string` 在文件里找不到"。也就是说，**选工具从来不是瓶颈，参数层的精确复现才是**。

#### 另一个例子：改个名字就掉分

有个基准（RoTBench）专门测这件事：把一个工具从 `Get_Weather` 改名成 `ABC`，功能一个字没变，模型就用不明白了。

还有工作发现，给测试用例平均只**多加三个语义相近的工具**，模型就开始出四类错：选错函数、生成的函数数量不对、参数赋错、参数幻觉。

再补一条更狠的证据：模型会**调用当前工具列表里根本不存在的工具**——因为它在训练时见过。这个现象在 GPT、Claude、Llama、Qwen 上都有记录。这说明工具名字是**住在权重里**的，不是被 prompt 临时告知的一个符号。

#### 为什么会这样（机制）

选工具本质上是一次软检索：key 是 `工具名 + 描述 + schema` 拼起来的东西，query 是当前上下文里的意图。**名字在这个 key 里信息密度最高、长度最短，所以扰动它的边际效应最大。**

`search_files` 落在模型见过几百万次的地方；`mcp__acme__srch_fs_v2` 落在一个没有任何邻居的地方，全部语义只能靠描述撑着。

#### 关键补充：分布是被造出来的，不是恰好的

`Read`、`Bash`、`str_replace` 这些不是"碰巧"落在分布上的。逻辑链是：

```
头部厂商定 harness 约定
  → 兼容它成为其他厂商"平替"的准入条件
  → 各家针对性后训练
  → 这些名字被写进所有模型的权重
```

这个链条是公开可见的：DeepSeek、GLM、Kimi、MiniMax 都提供了 Anthropic 兼容端点，就是为了能直接跑 Claude Code。

**所以"用 Claude Code 那套工具名"不是跟风，是有机制解释的工程判断。这个区别在面试里很重要——见 1.2。**

#### 但要注意边界

正典优势只覆盖那几个标准工具。你自研的 `query_risk_book`、`fetch_position_snapshot` 永远不会进任何人的后训练，那里没有"贴合"可言，只有语义透明 + 在描述里放示例。

完整的原则是：

> **有正典的整套照抄（连参数名一起），没正典的最大化自然语言可理解性。中间地带——把标准操作改名成自研叫法——是唯一必错的选择。**

为什么中间地带最差：用 `Edit` 这个名字但参数叫 `find`/`replace`（而不是 `old_string`/`new_string`），大概率**比换个全新名字更糟**。因为你激活了模型记忆里的一整套行为模式，然后违反了它。**激活强先验再打脸，是一个独立的失败模式。**

---

### 1.2 尺子：精巧度 vs 贴合度

这是这份修订说明的核心。宝典现在缺的不是知识点，是**一把统一的尺子**。

> **harness 的价值不在结构精巧度，在分布贴合度。**

有了这句话，宝典里散落的所有批判就串成一条线了——它们其实都在骂同一件事：

| 被骂的对象 | 精巧度 | 贴合度 |
|---|---|---|
| 图记忆 / A-MEM | 高（时序失效、实体消歧、关系推理） | 低（模型要先理解图结构本身） |
| MEMORY.md | 极低（就一个文本文件） | 极高（模型读了一辈子 markdown） |
| LangChain 的抽象层 | 高 | 低（你不知道模型实际收到了什么） |
| A2A 多 agent 协作 | 高 | 低 |
| 自研编辑格式 | 高（更紧凑、支持多处编辑） | 低（成功率崩） |
| `str_replace` | 低（笨，一次只改一处） | 极高 |

宝典 05 章原本就写了一句"**markdown 不需要被理解，图谱需要**"——那句话就是贴合度的定义，只是没给它命名。

#### 给尺子加刻度（重要）

"贴合分布"如果不给定义，会退化成"跟着 Claude Code 抄"，那就绕回原点了。加两条：

1. **可测的才叫贴合，不可测的叫模仿。** 任何贴合度主张都能做 A/B：改个工具名、换个编辑格式、加十个 skill，测成功率和方差。
2. **测的时候看方差和尾部，不是均值。** 工具选择失败往往不是均匀掉 5%，而是在某一类任务上直接塌成 0。所以要看 per-task 的成功率分布，不是总体准确率。而且这是高噪声实验，单跑一次差几个点说明不了任何事。

---

## 二、v1 清单（已全部执行，存档）

**必改（事实错误）——已改：**

- ✅ 02 Q1：MCP 定义改为「发现与调用协议（stdio/HTTP）」，补「模型看不见 MCP」
- ✅ 02 Q1：删「发明协议的人自己都不玩这个了」，改为「退场的是 model-facing 工具面，连通层成了 skill 分发通道」+ GitHub MCP 5 万 token vs SKILL.md 200 token
- ✅ 02 Q1：skill 机制改为「归约到 Read+Bash；税从结构化通道搬到自然语言通道，可预测失败（语义竞争）」
- ✅ 04 Q2：长期记忆位置改为「可变内容放稳定前缀之后」，修复与 Q3 的自相矛盾
- ✅ 01 Q4：goal hook 改为「hook 是任意 shell 命令，外部证据要自己写」+ 保留 goal-hook-套娃梗
- ✅ 03 Q1：删掉「2020 年三段式」半句

**要改（自相矛盾/表述不准）——已改：**

- ✅ 01 Q1 立尺子：「harness 抬不高能力上限，但决定能否持续待在上限附近；收益最高的三件事都在把模型拉回分布内」+ 追问加「精巧输给贴合」三例
- ✅ 05：「模型足够强之后」加作用域（仅限 memory 场景）+ 可观测退化信号判据
- ✅ 01 Q5：grep/find/ls 保留理由改为「三条实测 + 一条推测」，标注混讲的危害
- ✅ 01 Q4：死循环段改正确因果（compact 移除天然刹车）；二分排查树加入 01 Q5 追问
- ✅ 06：白马二加 ⚠️ 读者提示（没做过就改成方案设计讲）

**可选（02 章重写方向）——已执行：** 02 Q1 扩展分析改为机制论证（命名空间前缀推离分布、供应商写描述、一次挂几十个），删掉纯体感的可调试性批评。

## 三、v2 清单（本轮，已执行）

**删改副作用——悬空引用（6 处）：**

- ✅ 03 Q1 追问不再引用已删的「三段式」，改为「把 RAG 当成需要专门架构的东西过时了」
- ✅ 09 Q1 删掉「MCP hang 住干瞪眼」类比（与 02 新口径打架）
- ✅ 07 统一为「遇冷/落地稀少」；「问题是想象的」改为「需求真实但优先级低，协议先于需求落地」（否则与大厂联署对撞）
- ✅ 03 Q3「见 06」→「见 08/09」（章节号错）
- ✅ 06 Q1「和 06 一脉相承」→「和 08 一脉相承」（自引用）
- ✅ 01 Q5 前段「蒸馏 claude……一定是强力支持」降调为推测，力气押在后面四条理由上

**遗留修正：**

- ✅ 06 章头：「段子」→「案例」；「Codex 专用压缩接口」降级为待核实表述
- ✅ 08 Q3 接上尺子：「差距主要来自贴合度而不是精巧度——把 CI/测试/review 接进 loop，不是发明新抽象」
- ✅ 02 Q3 排查流程指向 01 Q5 二分树，不再重复写一套
- ✅ 04 Q3 删「好像」，改为「各家实现不同（显式断点/自动前缀），共同前提是前缀稳定」
- ✅ 01 Q2「全面转向 skill」→「把这个思路做成了 skill 形态」
- ✅ 02 Q1 简答压缩成三句对照（二十秒能念完），机制细节移到扩展分析

**新补内容：**

- ✅ 01 Q5 补**长尾边界**：「有正典的整套照抄（连参数名），没正典的最大化语义透明，中间地带唯一必错」+「激活强先验再打脸是独立的失败模式」
- ✅ 09 Q2 补 reward hacking 下半句：判定器身份切换（eval 时没人优化它，reward 时正对着它优化）+ 追问「怎么防」（对抗测试 + reward 分布尖峰监控）

## 四、待核实（用之前自己查）

| 位置 | 待核实内容 |
|---|---|
| 01 Q1 | kimi-k3 技术报告里的"类似观察"——型号是否存在、报告里是否真有这句 |
| 01 Q5 | Vercel text-to-sql 砍 80% 工具后成功率 80%→100%——100% 太干净，要说清是什么 benchmark、什么口径 |
| 01 Q4 | Codex 的 `/goal` |
| 06 Q1 | Codex 的"专用压缩接口"（章节头已降级为待核实表述） |
| 07 | Anthropic 用一队并行 Claude 写出 C 编译器 |
| 07 | Claude Code 的 "agent teams"——功能名和形态 |
| 08 Q3 | Harness Engineering 的出处表述（已软化为"被 OpenAI 一篇文章带火"） |
| 05 | "Claude Code 的长期记忆核心就是一个 markdown 文件"（已软化） |

**已核实为真**：CVE-2025-68664（LangChain 序列化注入，CVSS 9.3，2025-12-23 公布，`dumps()`/`dumpd()` 不转义含 `lc` 键的字典，可泄露环境变量甚至 RCE；影响 langchain-core < 0.3.81 及 1.0.0–1.2.5）。

> **只报 CVE 编号、答不出是什么漏洞，比不提更难看。** 要用就把机制记住。

## 五、本轮未处理

- **08 Q2（sandbox）**：「只有对延迟极度敏感的场景才值得做 microVM」因果可能反了——microVM/gVisor 的首要理由是**威胁模型**（Docker 共享内核，对真正不可信的代码不是安全边界），延迟是次要理由。待原作者确认后再改。

---

## 附：改完之后的一句话总纲

如果只记一句话，记这个：

> **agent 工程的收益不来自结构的精巧，来自轨迹对模型后训练分布的贴合。所有"看起来更聪明"的设计，都要先回答一个问题：模型见过这个吗？**

宝典里所有的批判——图记忆、A2A、LangChain、多 agent 角色扮演、自研格式——本质上都是这一句话的推论。把它写在最前面，整份文档会从"一堆观点"变成"一个判断框架"。
